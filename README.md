# Field Flight
Small browser-based Flappy Bird clone built with vanilla HTML/CSS/JS, with a
more natural field-guide visual direction.

## Run locally
Open `index.html` directly in a browser, or serve the folder with a simple static server:

```sh
python3 -m http.server 8000
```

Then open <http://localhost:8000/>.
